[workspace]
members = ["contracts/vault"]
resolver = "2"

[workspace.package]
edition = "2021"
rust-version = "1.84"
license = "Apache-2.0"

[profile.release]
opt-level = "z"
overflow-checks = true
debug = 0
strip = "symbols"
lto = true
codegen-units = 1
panic = "abort"